delay_notify:
  alias: Delay Notify
  sequence:
    - service: notify.discord_webhook
      data_template:
        message: >
          {{ airport }} went into a {% if type == 'GDP' %} ground delay program! {% elif type == 'GS' %} ground stop that ends at {{ endtime }}! {% endif %}
